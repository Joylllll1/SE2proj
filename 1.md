# 移动端 UI 适配 — 完成总结

> `feature/mobileUI` 分支，18 个文件，纯 CSS/布局改造。未修改后端、未新增依赖、未重写组件结构。

---

## 改动总览

| 步骤 | 内容 | 文件数 | 状态 |
|------|------|--------|------|
| 第一步 | 导航系统（激活 MobileNav + TopBar 适配） | 5 | ✅ |
| 第二步 | PostCard + Comment + ReplyCard 响应式 | 3 | ✅ |
| 第三步 | ComposePage + FAB + 收藏/点赞 + HomePage | 5 | ✅ |
| 第四步 | DetailPage + 模态框 + Announcements + 认证 | 5 | ✅ |
| 第五步 | AdminDashboard 最低限度适配 | 1 | ✅ |
| 第六步 | Toast 偏移 + 全局验证 | 1 | ✅ |

**合计：18 个文件，+78 行 / -62 行，ESLint 零报错。**

---

## 第一步：导航系统（P0 阻塞级）

### MobileNav.jsx
- `max-lg:hidden` → `lg:hidden`（修复：原来在移动端不可见，断点写反了）
- 导航项保持原有的 4 个（首页、收藏、公告、设置）

### Sidebar.jsx
- `max-md:hidden` → `max-lg:hidden`（< 1181px 隐藏，避免与 MobileNav 在 901-1181px 区间重叠）

### TopBar.jsx
- 顶部栏高度：`max-sm:h-[48px]`，内边距 `max-sm:px-3`
- 按钮间距：`max-sm:gap-1.5`
- 设置按钮：`max-sm:hidden`（移动端由 MobileNav 提供入口）

### App.jsx
- 新增 `MobileNav` 引入和渲染（之前是死代码，从未 import）
- 新增 `Icon` import（供 FAB 使用）

### tailwind.css
- `.sidebar`：`max-md:hidden` → `max-lg:hidden`（与 JSX 同步）
- `.mobile-nav`：`max-lg:hidden` → `lg:hidden`，`grid-cols-5` → `grid-cols-4`（与 JSX 同步）

---

## 第二步：核心内容卡（P0 阻塞级）

### PostCard.jsx（9 处）

| 属性 | 桌面 | 移动端 (< 681px) |
|------|------|------------------|
| 卡片内边距 | `p-[18px_20px_14px]` | `max-sm:p-3` |
| 头部间距 | `gap-3.5` `mb-4` | `max-sm:gap-2.5` `max-sm:mb-3` |
| 用户区块间距 | `gap-[11px]` | `max-sm:gap-2` |
| 匿名头像 | `w-[38px] h-[38px]` | `max-sm:w-8 max-sm:h-8` |
| 标题（非 compact） | `text-xl` | `max-sm:text-base` |
| 正文 | `text-[15px]` | `max-sm:text-[13px]` |
| 图片网格 | 按数量 2/3 列 | `max-sm:grid-cols-1` |
| 底部栏 | `px-5` `gap-3.5` | `max-sm:px-3` `max-sm:gap-2` |
| 标签 / 指标 | `gap-2.5` | `max-sm:gap-1.5` / `max-sm:gap-2` |

### Comment.jsx（5 处）
- 外层间距：`gap-[12px]` → `max-sm:gap-2`
- 头像：`w-[34px] h-[34px]` → `max-sm:w-7 max-sm:h-7`
- 评论气泡：`p-4` → `max-sm:p-3`
- 操作按钮：`gap-[14px]` → `max-sm:gap-2`
- 回复输入框：`p-[14px]` → `max-sm:p-3`

### ReplyCard.jsx（5 处）
- 与 Comment 同样的间距 / 头像 / 内边距 / 按钮缩放策略

---

## 第三步：关键页面（P1 高优先级）

### ComposePage.jsx（9 处）

| 属性 | 桌面 | 移动端 |
|------|------|--------|
| 标题区域布局 | `flex items-center justify-between` | `max-sm:flex-col max-sm:items-stretch` |
| 标题输入框 | `text-xl` `p-[18px_20px]` | `max-sm:text-lg` `max-sm:p-[14px]` |
| 正文 textarea | `min-h-[240px]` `p-5` | `max-sm:min-h-[180px]` `max-sm:p-3` |
| 工具栏 | `gap-3` `p-[14px_20px]` | `max-sm:gap-1.5` `max-sm:p-3` |
| 图片预览网格 | 多列 | `max-sm:grid-cols-1` |
| 情绪选择器 | `gap-3` `p-[14px_20px]` | `max-sm:gap-1.5` `max-sm:p-3` |
| 发布行 | `p-[14px_20px]` | `max-sm:p-3` |

### FAB 撰写按钮（App.jsx）
- 使用已有的 `.fab-compose` CSS 类（`hidden max-lg:grid`）
- 移动端底部偏移 `max-sm:bottom-[90px]`，避开 MobileNav
- `activePage !== 'compose'` 时渲染（在编辑页自动隐藏）
- 管理员和未登录用户路径自动排除（代码分支决定）

### BookmarksPage.jsx（3 处）
- 瀑布流列间距：`[column-gap:18px]` → `max-sm:[column-gap:12px]`
- 文件夹筛选按钮：`gap-3` → `max-sm:gap-1`

### LikesPage.jsx（1 处）
- 瀑布流列间距：同上

### HeroCarousel.jsx（1 处）
- 幻灯片：`min-h-[280px]` → `max-sm:min-h-[200px]`
- 间距：`gap-6` `p-7` → `max-sm:gap-4` `max-sm:p-4`

---

## 第四步：详情与交互（P2 中等优先级）

### DetailPage.jsx（7 处）

| 属性 | 桌面 | 移动端 |
|------|------|--------|
| 详情卡片 | `p-[18px]` | `max-sm:p-3` |
| 元信息间距 | `gap-2` | `max-sm:gap-1` |
| 帖主管理 | `flex items-center` | `max-sm:flex-col max-sm:items-stretch` |
| 评论输入区 | `p-[14px]` | `max-sm:p-3` |
| 评论 textarea | `min-h-[86px]` | `max-sm:min-h-[60px]` |
| 发送按钮 | `px-[18px]` | `max-sm:px-3 max-sm:text-sm` |
| 图片预览 | `max-w-[200px] max-h-[150px]` | `max-sm:max-w-[120px] max-sm:max-h-[90px]` |

### ReportModal.jsx（2 处）
- 弹窗内边距：`p-6` → `max-sm:p-4`
- 按钮组：`max-sm:flex-col max-sm:gap-2`

### ConfirmLeaveDialog.jsx（2 处）
- 顶部：`px-6 pt-6 pb-4` → `max-sm:px-4 max-sm:pt-4 max-sm:pb-3`
- 底部：`px-6 py-5` → `max-sm:px-4 max-sm:py-3`

### AnnouncementsPage.jsx（4 处）
- 页面头部：`max-sm:flex-col max-sm:items-stretch max-sm:gap-3`
- 我的申请卡片：`max-sm:flex-col max-sm:items-start`
- 详情弹窗 / 发布弹窗：`p-6` → `max-sm:p-4`
- 海报预览：`h-[240px]` → `max-sm:h-[180px]`

### Auth 页面（tailwind.css）
- `@media (max-width: 680px)` 内新增 `.auth-email-select { w-[140px] }`，防窄屏溢出

---

## 第五步：管理端（P3 低优先级）

### AdminDashboard.jsx（5 处）
- 审计日志表格：`overflow-x-auto`（横滚支持）
- 举报 / 封禁 / 拒绝卡片头部：`max-sm:flex-col max-sm:gap-2`（3 处）
- 活动子标签：`max-sm:gap-1`

> 管理端在计划中标注为 P3，本次仅做最低限度防崩溃处理。

---

## 第六步：全局样式收尾

### Toast.jsx
- `bottom-[100px]` → `bottom-[100px] max-sm:bottom-[120px]`（避免被 MobileNav 遮挡）

### 已有保障（无需改动）
- `body { min-width: 320px }` ✅
- `main` 已有 `max-md:pb-24`（给 MobileNav 留空间）✅
- Auth 页面已有 `@media (max-width: 680px)` 处理 ✅
- HomePage 右侧栏已有 `max-lg:static max-lg:grid-cols-3 max-sm:grid-cols-1` ✅

---

## 设计决策

1. **断点统一使用 `max-sm:`（< 681px）**：与 Tailwind v4 项目配置的 `--breakpoint-sm: 681px` 一致。少数场景保留 `max-md:`（901px）作为中间态（如 HeroCarousel 的 `max-md:grid-cols-1`），这是原有代码逻辑。

2. **所有修改均为增量式**：不重写组件，不改变 props 接口，只在 className 中追加 `max-sm:*` 或 `max-lg:*` 响应式类。

3. **不引入 JS 媒体查询**：全部使用 Tailwind 响应式前缀，保持与项目现有模式一致。

4. **MobileNav / Sidebar 断点分界点为 1181px（`lg`）**：Sidebar 在 >= 1181px 显示，MobileNav 在 < 1181px 显示。两者互斥，避免同时出现。

5. **FAB 按钮复用已有 CSS 类**：`.fab-compose` 已在 tailwind.css 定义，仅新增 `max-sm:bottom-[90px]` 偏移。显示逻辑上，由于 App.jsx 中管理员和未登录用户走独立分支，FAB 自动排除这些页面。

---

## 验证清单

- [x] ESLint 零报错（所有 18 个文件）
- [x] 未修改任何后端代码
- [x] 未新增 npm 依赖
- [x] 未引入新的 React 组件（FAB 是已有 CSS 类 + 原生 button）
- [x] 未修改 Zustand store、API 服务、路由逻辑
- [x] 所有改动均为 className 字符串追加
- [x] 移动端访问方式：`npm run dev -- --host`，手机同 WiFi 访问 Network IP
